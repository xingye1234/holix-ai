{
  "name": "web_search",
  "version": "1.1.0",
  "description": "Search and read web pages via Zhipu BigModel Tool API (web_search + web_browser). Use when the user needs up-to-date internet information, recent news, or content from a specific URL.",
  "prompt": "When freshness matters, call web_search before answering. If the user provides a specific URL or asks for page details, call web_browser to read the webpage content. If API credentials are missing, ask the user to configure the built-in web_search skill in Settings > Skills.",
  "config": [
    {
      "key": "apiToken",
      "label": "BigModel API Token",
      "type": "string",
      "description": "用于 Authorization: Bearer <token> 的鉴权令牌",
      "placeholder": "输入 BigModel token（不含 Bearer 前缀）",
      "required": true,
      "secret": true
    },
    {
      "key": "searchEndpoint",
      "label": "Web Search API Endpoint",
      "type": "string",
      "description": "默认使用 BigModel 官方 web_search 接口地址",
      "default": "https://open.bigmodel.cn/api/paas/v4/web_search"
    },
    {
      "key": "browserEndpoint",
      "label": "Web Browser API Endpoint",
      "type": "string",
      "description": "默认使用 BigModel 官方 web_browser 接口地址（网页阅读）",
      "default": "https://open.bigmodel.cn/api/paas/v4/web_browser"
    },
    {
      "key": "defaultEngine",
      "label": "Default Search Engine",
      "type": "select",
      "description": "未显式指定时使用的搜索引擎",
      "default": "search_std",
      "options": [
        { "label": "search_std", "value": "search_std" }
      ]
    }
  ],
  "tools": [
    {
      "type": "js",
      "name": "web_search",
      "description": "调用 BigModel web_search 接口执行联网检索，支持 query、数量、时间范围与站点过滤。",
      "dangerous": true,
      "file": "index.js",
      "schema": {
        "search_query": { "type": "string", "description": "搜索关键词或问题描述" },
        "search_engine": { "type": "string", "description": "搜索引擎（默认 search_std）", "optional": true },
        "search_intent": { "type": "boolean", "description": "是否启用搜索意图识别（默认 false）", "optional": true },
        "count": { "type": "number", "description": "返回结果条数（1-50，默认 10）", "optional": true },
        "search_domain_filter": { "type": "string", "description": "域名过滤（可选）", "optional": true },
        "search_recency_filter": { "type": "string", "description": "时效筛选（默认 noLimit）", "optional": true },
        "content_size": { "type": "string", "description": "内容粒度（low|medium|high，默认 medium）", "optional": true },
        "request_id": { "type": "string", "description": "请求 ID（可选）", "optional": true },
        "user_id": { "type": "string", "description": "用户 ID（可选）", "optional": true }
      },
      "permissions": {
        "allowedBuiltins": ["node:https", "node:url"],
        "timeout": 30000
      }
    },
    {
      "type": "js",
      "name": "web_browser",
      "description": "调用 BigModel web_browser 接口读取指定 URL 的网页内容（网页阅读）。",
      "dangerous": true,
      "file": "index.js",
      "schema": {
        "url": { "type": "string", "description": "要阅读的网页 URL" },
        "count": { "type": "number", "description": "需要返回的内容条目数量（默认 3）", "optional": true },
        "search_recency_filter": { "type": "string", "description": "时效筛选（默认 noLimit）", "optional": true },
        "content_size": { "type": "string", "description": "内容粒度（low|medium|high，默认 medium）", "optional": true },
        "request_id": { "type": "string", "description": "请求 ID（可选）", "optional": true },
        "user_id": { "type": "string", "description": "用户 ID（可选）", "optional": true }
      },
      "permissions": {
        "allowedBuiltins": ["node:https", "node:url"],
        "timeout": 30000
      }
    }
  ]
}
