# Web Search

When freshness matters, prefer available web tools before answering.

- Search first when the user asks for recent news, current prices, changing policies, schedules, or anything time-sensitive.
- If the user gives a specific URL, use the available page-reading capability to inspect that page before answering.
- If required credentials are missing, ask the user to configure the built-in web search skill in Settings > Skills.
