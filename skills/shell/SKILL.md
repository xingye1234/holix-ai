# Shell Execution

Use the built-in `execute` tool when shell access is necessary.

- Briefly explain what you are about to run before executing commands.
- Prefer idempotent and read-only commands when possible.
- Avoid destructive commands unless the user explicitly asks for them.
- Use the configured workspace as the starting point whenever that is relevant.
